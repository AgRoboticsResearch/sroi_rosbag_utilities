[project]
name = "piper-sroi-convert"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "lerobot[dataset]",
    "numpy",
    "Pillow",
    "scipy",
]

[tool.uv.sources]
lerobot = { path = "lerobot" }
